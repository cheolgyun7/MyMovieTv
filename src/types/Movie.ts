// src/types/Movie.ts
export type Movie = {
    id: number;
    title: string;
    overview: string;
    poster_path: string;
    release_date: string;
    backdrop_path: string;
    // 필요한 다른 필드들을 추가하세요
  }
  

  export type TV = {
    id: number;
    name: string;
    overview: string;
    poster_path: string;
    release_date: string;
    backdrop_path: string;
    // 필요한 다른 필드들을 추가하세요
  }
  